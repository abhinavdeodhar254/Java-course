public class FinallyWithResources {
    
}
//program is always in stack part of the resources.
//Programs run inside the main memory. this main memory is in part of stack and heap. main context of the program is in stack.
//heap is external to program. 
//Objects that we create that are created in the heap.
//and this objects in the heap are accessed from the references that we create in the stack.
//so program is using the resources in the heap in the form of obects.

//database is also a resource. 
//this resource must close after the use is done.
 
